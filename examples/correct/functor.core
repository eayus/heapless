λ w.

    type Functor :: (Type -> Type) -> Type = λ F. ∀ A :: Type. ∀ B :: Type. (A -> B) -> F A -> F B;

    let void : ∀ F :: Type -> Type. ∀ A :: Type. Functor F -> F A -> F Unit =
        Λ F. Λ A. λ fmap. λ x. fmap @A @Unit (λ v. unit) x;

    w
