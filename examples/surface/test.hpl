let id : a -> a =
    \x => let y = x; y;

let const : a -> b -> a =
    (\x y => x);

let rec composeN : Int -> (a -> a) -> a -> a =
    \n f x => if n >= 0
        then f (composeN (n - 1) f x)
        else x;